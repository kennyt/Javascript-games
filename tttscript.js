var s = require('./tictactoe.js');
var util = require('util');
var t = s.makeTTT();
require('prompt');

console.log(t);
console.log("hello!")

while (true) {
  console.log("x's turn", util.inspect(text));

}